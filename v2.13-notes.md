# Millie 2.13

**Block extensions on specific sites.** The Extensions menu now has a "Block extensions on this site" toggle. Turn it on and every extension stops running on that site (its content scripts won't inject there) — while staying fully active everywhere else. Useful for sites that misbehave when extensions like Grammarly or 1Password inject into them. The setting is per-site and remembered.
