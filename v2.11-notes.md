# Millie 2.11

**Right-click now respects each site's own menu.** Apps that provide their own context menu — n8n, Google Docs/Sheets, Figma, and similar — now show *their* menu on right-click instead of Millie's. Millie's menu still appears everywhere a site doesn't handle the right-click itself (plain pages, text, links, images).
